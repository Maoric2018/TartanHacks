{
  "name": "neon-phases",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "size": "node scripts/size-report.mjs",
    "build:size": "npm run build && npm run size"
  }
}
